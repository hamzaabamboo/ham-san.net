---
export const prerender = false;

const { params } = Astro;
const { locale } = params;

return Astro.redirect(`/${locale}/namecard/default`);
---

<meta http-equiv="refresh" content={`0;url=/${locale}/namecard/default`} />
